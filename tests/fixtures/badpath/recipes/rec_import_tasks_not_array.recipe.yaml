# VIOLATION: The 'tasks' import must be an array, but is an object.
meta:
  id: rec_import_tasks_not_array
  name: "Recipe with Invalid Tasks Import"
imports:
  tasks:
    block_id: blk_task
    variant_id: explain_code
composition:
  sequence:
    - block_ref: "tasks[0]"
