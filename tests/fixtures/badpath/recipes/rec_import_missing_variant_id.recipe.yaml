# VIOLATION: The 'persona' import is missing the required 'variant_id' property.
meta:
  id: rec_import_missing_variant_id
  name: "Recipe with Incomplete Import"
imports:
  persona:
    block_id: blk_persona
    # variant_id is missing
composition:
  sequence:
    - block_ref: "persona"
