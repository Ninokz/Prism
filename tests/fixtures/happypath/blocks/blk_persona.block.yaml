# PURPOSE: Tests Block/Variant default value merging.
meta:
  id: blk_persona
  name: "AI Persona Definitions"
block_type: Persona
defaults:
  language: "JavaScript" # Block-level default
variants:
  - id: expert_teacher
    defaults:
      language: "TypeScript" # Variant-level override
      teaching_tone: "strict"
    template_id: tpl_persona_expert
    # No contract_id, as all variables are compile-time.
