# PURPOSE: Tests merging when only block-level defaults are present.
meta:
  id: blk_merging_block_only
  name: "Tests Merging Block-Only Defaults"
block_type: OutputSpecification
defaults:
  format: "json"
  indent: 2
variants:
  - id: variant_no_defaults
    # This variant has no 'defaults' key.
    template_id: tpl_output_json
